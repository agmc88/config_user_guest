---
language:   python
python:     2.7

before_install:
        - sudo apt update

install:
        - pip install ansible
        - "printf '[defaults]\nroles_path = ../' > ansible.cfg"


script:
        - ansible-playbook createFrench.lproj.yml --syntax-check
